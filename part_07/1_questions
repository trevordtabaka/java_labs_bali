1) What is a Java package?
groups of related classes

2) What is the CLASSPATH variable all about?


3) What does the package structure mirror?

directories

4 How do packages affect access to Java classes?
They can make classes or groups of classes private from the rest of the classes not in that package or subpackage

5) What is an interface?
An interface is similar to an abstract class in that it is a contract with classes that implement it to implement the members of the interface.
But classes a can implement more than one interface.

6) What makes interfaces unique?
Classes that implement an interface let you know you can interact with any of those classes in the same way as long as it is involving the interface implementation

7) What keyword is used to specify that a class is going to use a separate interface?
implement

8) What makes interfaces so useful?
You can take advantaeg of the polymorphism aspect where you have one interface but multiple ways the members are implemented

9) What was added in JDK 8 that makes interfaces even more useful?
default methods

10) How many interfaces can a single class implement?
largest int...basically unlimited

11)What is a default interface method?
A method in an interface that does not HAVE to be implemented by classes implementing the interface